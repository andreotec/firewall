   26  pacman -S vim git
   27  git clone http://github.com/andreotec/firewall
   39  git add firewall-andreo
   40  git status
   42  git config --global user.email "andreotec@gmail.com"
   43  git config --global user.name "andreotec"
   44  git commit -m "Novo script de firewall firewall-andreo"
   45  git push origin master
